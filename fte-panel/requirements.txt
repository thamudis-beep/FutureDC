# Pinned to the versions the pipeline was built and tested with (Python 3.11).
pandas==3.0.5
numpy==2.4.6
scipy==1.17.1
statsmodels==0.15.0
matplotlib==3.11.2
pyarrow==25.0.1
PyYAML==6.0.1
requests==2.33.1
# Optional: only needed when HEADCOUNT_LLM=1 (LLM fallback for headcount sentences)
anthropic==1.5.0
