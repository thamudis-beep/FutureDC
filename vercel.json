{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/", "destination": "/stack.html" },
    { "source": "/deck", "destination": "/index.html" }
  ]
}
